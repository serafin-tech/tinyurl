# Branch naming convention

feature branches with following prefixes:

- `feature/` for new functionality proposed in branch
- `bugfix/` for fixes for existing functionality
- `security/` for security related changes

two main branches to be miantained:

- `main` for stable, production-ready version of the application
- `develop` for development version of the application, application in this stage should be ready to be build
